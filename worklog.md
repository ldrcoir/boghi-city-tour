# Worklog — Boghi City Tour

---
Task ID: 9
Agent: main (Super Z)
Task: هنر جدید ماشین‌ها بر اساس رفرنس‌های تأییدشده کاربر + فیکس «دکمه‌ها کار نمی‌کنند» + موسیقی جدید + ریلیز v0.9

Work Log:
- کاربر ۴ رفرنس تأیید کرد (hero_car_A/B، track_tehran، garage_tuning) — درشتی + جزئیات + چشم + لبخند = بایبل استایل
- بازسازی ابزاربیلد بعد از ریست محیط: setup_toolchain.sh (گودو 4.7.2 + تمپلیت + build-tools 34 + platform 36 توسط گریدل) + JDK 21 با javac از Adoptium → /home/z/jdk + platform-tools + لایسنس‌های SDK
- ریشه‌یابی «دکمه‌ها کار نمی‌کنند»: CenterContainer تمام‌صفحه‌ی پنل مأموریت‌ها با mouse_filter پیش‌فرض STOP همه‌ی لمس صفحه را می‌بلعید (تست‌های قبلی فقط اسکرین‌شات بودند، هرگز تپ نمی‌کردند!) → فیکس: IGNORE + مخفی‌سازی center و ماشین‌های تزئینی در _show
- autotest جدید: تپ واقعی از پایپ‌لاین ورودی (InputEventMouseButton) روی دکمه مأموریت‌ها و برگشت — TAP-PLAY OK + TAP-BACK OK روی xvfb
- نگهبان چیدمان: فقط وقتی پنل خانه فعال است قضاوت می‌کند (رفع هشدار کاذب)
- تولید ۱۶ اسپرایت ماشین با z-ai image (استایل یکسان ۳/۴ رو به راست، بک‌گراند ماژنتا) + برش حرفه‌ای با rembg/U2Net (alpha matting) → 640px → 12 اسپرایت قدیمی جایگزین + 4 جدید
- ۴ ماشین مسابقه‌ای جدید در globals.gd: pride_blue (4800)، pejo_green (5400)، shahin_white (6000)، dena_red (6800)
- موسیقی v4 «ماریمبای محله» (make_music_v4.py): F ماژور 108BPM، ۳۲ میزان ۷۱.۱ ثانیه لوپ بی‌درز — ماریمبا/جعبه‌موزیک/یوکللی/بیس گرد/کیک نرم/شیکر/کلپ — تأیید طیفی: بدون باند بیپی غالب، داینامیک ۱۶.۹dB
- پس‌زمینه منو = نسخه نقاشی track_tehran کاربر (1280×720 webp 101KB) — پس‌زمینه قبلی 1.6MB حذف؛ گاراژ webp 171KB — صرفه‌جویی ~3MB
- رفع مشکلات بیلد اندروید: تمپلیت در android/build/ + .build_version + .gdignore (بدون آن ایمپورتر محتوا را کپی تودرتو می‌کند!) + javac/JDK + پاکسازی فایل‌های gradle سرریزشده از ریشه game/
- APK: versionCode 10 / versionName 0.9، 67,861,166 بایت، sha256 ea883f9d...، STORE assets (138) + zipalign + apksigner
- تست دسکتاپ: منو/مأموریت‌ها/گاراژ/گیم‌پلی با اسپرایت‌های جدید — همه سالم
- ریلیز گیت‌هاب v0.9 (id=394103700) + جایگزینی assetهای همه ریلیزهای قدیمی (v0.3…v0.8، شامل id=393983565) با بایت v0.9 — تأیید API: همه 67,861,166 uploaded + تأیید دانلود: sha256 لینک latest مطابق
- docs/index.html + README به‌روز شد؛ .gitignore بازسازی شد (env/secrets/download/tools/car_gen)
- یادآوری: PAT در .secrets.env از دست رفته بود؛ از توکن URL ریموت بازیابی شد (دسترسی admin)

Stage Summary:
- تحویل v0.9: هنر یکدست ۱۶ ماشین، ۴ مسابقه‌ای جدید، موسیقی v4، پس‌زمینه نقاشی منو، فیکس ورودی منو
- درس کلیدی: هر Control تمام‌صفحه با mouse_filter=STOP (پیش‌فرض) دکمه‌های زیرش را می‌کشد؛ تست واقعی تپ لازم است نه فقط اسکرین‌شات
- درس کلیدی ۲: پوشه‌ی android/build حتماً .gdignore می‌خواهد
- درس کلیدی ۳: پروسه‌های پس‌زمینه در این محیط کشته می‌شوند — کار طولانی باید foreground chunked یا setsid باشد
- وضعیت: منو روی گوشی کاربر بالا می‌آید (اسکرین‌شات v0.8) و دکمه‌ها در v0.9 باید لمس را دریافت کنند
